class Table:
    __init__(self):
        
    #Create some internal variables that can be used to store information.
    read(self, filename):
        fin = open('ex4input.txt','r')
        for i in fin:
        line
    #Read the data in the given file and store them within the class instance.
    set_key(self, key_attribute)
    #Set the given attribute as key attribute.
    get_key(self)
    #Return the current key attribute name.
    get_attributes(self)
    get_key_values(self)
    key_search(self, key_value)
    

     